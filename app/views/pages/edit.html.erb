<% content_for :title, page_title("Edit #{@page.title}") %>

<h1>Edit Page</h1>

<%= render "form", page: @page %>

<div class="form-links">
  <%= link_to "View page", public_page_path(@page.slug), class: "btn btn--ghost" %>
  <%= link_to "Back to pages", pages_path, class: "btn btn--ghost" %>
</div>
